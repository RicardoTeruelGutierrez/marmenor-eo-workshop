# ── Python ─────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/

# ── Jupyter ────────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb_checkpoints

# ── Editors / OS ───────────────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# ── Credentials (NEVER commit) ─────────────────────────────────────────
.env
.env.*
*.pem
*.key
secrets.json

# Claude Code local config — contains approved command history that may
# include OAuth client secrets passed via env vars. NEVER commit.
.claude/

# ── Generated by the workshop (regenerate from scripts) ────────────────
# Real Sentinel-2 GeoTIFFs (~140 MB) — get them with:
#   python scripts/download_sentinel2.py
data/s2_*.tif

# SQLite DB + Parquet rasters built by Module 3
database/

# Temporary slide preview folder (PowerPoint COM exports)
slides/_preview/

# Notebook HTML exports — commented in: useful for attendees who can't run Python.
# Uncomment the lines below if you prefer to keep them out of the repo.
# notebooks/*.html
